# 全站版本文案清理

已检查全部 796 个中英文内容页面，包括 MDX 首页、数据库总览及条目、成就目录、攻略、安装、关于、更新说明与署名页。此前遗漏的首页内容基线、成就片段和生成器摘要已修正。

读者可见的标题、正文、卡片、表格、SEO 描述、社交分享描述和结构化数据不再显示游戏版本、构建号或成就版本标签。移除输出 HTML 中的游戏版本和构建号 meta 标签；`llms.txt` 同步更新。游戏规则集、玩法条件和设备最低系统要求保留。

内部快照、源码校验、素材清单及历史维护记录仍保留来源版本，用于复现数据和署名，不通过文章模板显示。

`check-site.py` 新增对每个构建页面文字和 meta 内容的版本表述检查。修复前，该检查识别出 796 页存在版本文字或版本元数据；修复后完整 `pnpm validate` 通过：796 内容页、794 可索引页、2 页排除，错误为空。重复生成、图标覆盖及来源校验回归通过。浏览器确认中文首页原内容基线已替换为 TestFlight 安装可用性说明。

[完整验证日志](validation/version-wording/validate.log)。
